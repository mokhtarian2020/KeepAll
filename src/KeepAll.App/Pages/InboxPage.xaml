<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KeepAll.App.Pages.InboxPage"
             Title="Inbox">
  <Grid RowDefinitions="Auto,Auto,*">
    <Grid Padding="4,0,4,8" Row="0">
      <Label Text="KeepAll" FontSize="28" FontAttributes="Bold" />
      <Label Text="Save anything in one tap" FontSize="12" TextColor="{StaticResource TextSecondary}" />
    </Grid>

    <HorizontalStackLayout Grid.Row="1" Spacing="8">
      <Button Text="+ Add" Clicked="OnAddClicked"/>
      <SearchBar x:Name="SearchBox" Placeholder="Search title or note" TextChanged="OnSearchChanged" />
    </HorizontalStackLayout>

    <CollectionView x:Name="ItemsView" Grid.Row="2" ItemsSource="{Binding Items}" SelectionMode="None">
      <CollectionView.ItemTemplate>
        <DataTemplate>
          <Grid Padding="10" ColumnDefinitions="Auto,*,Auto" BackgroundColor="{StaticResource SurfaceElevated}" Margin="0,8" >
            <Frame WidthRequest="44" HeightRequest="44" CornerRadius="8" Padding="0" HasShadow="False" BackgroundColor="#EEF2FF">
              <Image Source="{Binding CoverUrl}" Aspect="AspectFill"/>
            </Frame>
            <VerticalStackLayout Grid.Column="1" Spacing="2" Padding="8,0">
              <Label Text="{Binding Title}" FontAttributes="Bold"/>
              <Label Text="{Binding SourceApp}" FontSize="12" TextColor="{StaticResource TextSecondary}" />
            </VerticalStackLayout>
            <VerticalStackLayout Grid.Column="2" Spacing="6" HorizontalOptions="End" VerticalOptions="Center">
              <Button Text="Open" Clicked="OnOpenClicked"/>
              <ImageButton Source="more.png" Clicked="OnMoreClicked" />
            </VerticalStackLayout>
          </Grid>
        </DataTemplate>
      </CollectionView.ItemTemplate>
    </CollectionView>
  </Grid>
</ContentPage>
